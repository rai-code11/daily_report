## 取り組んだ課題一覧
- Django_Xクローン

## わかったこと
自分でハッシュ値を作って fixture に貼る
「ハッシュ値だけ作って JSON に入れたい」ならこれ。
1) ハッシュ生成（shell）
```python
python manage.py shell

from django.contrib.auth.hashers import make_password
make_password("testA")
```

1) Djangoでは user というフィールド名から自動で user_id 列が作られる
つまり、こう書けば…
```python
user = models.OneToOneField(settings.AUTH_USER_MODEL, ...)
```
DBの列名は内部的に user_id になる。 だからフィールド名まで user_id にする必要がない。

## 次やること
- Django Xクローン


## 感じたこと
- 実装のイメージは思い描けてきてるので、明日その構想を形にする

## 学習時間
- 4.10h
