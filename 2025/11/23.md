## 取り組んだ課題一覧
- チェックアウトメール機能

## わかったこと
- メール送信設定
**settings.pyの設定**
```pyhon
# 本番環境用のメール設定
EMAIL_BACKEND = "django.core.mail.backends.smtp.EmailBackend"

EMAIL_HOST = "smtp.sendgrid.net"
EMAIL_PORT = 587
EMAIL_HOST_USER = "apikey"
# ここにapi-keyを入れる
EMAIL_HOST_PASSWORD = env("SENDGRID_API_KEY")
EMAIL_USE_TLS = True
DEFAULT_FROM_EMAIL = "配信アドレス"
```

- 定義する(views.py)
```
      send_mail(
        subject="ご注文ありがとうございます",
        message=message,
        from_email=settings.DEFAULT_FROM_EMAIL,
        recipient_list=[checkout.email],
    )
```

## 次やること
- プロモーションコード
- PR返答なかったらJS_カレンダー


## 感じたこと
- DjangoはDB設計とクエリ表記をマスターすればもっとサクサクと構築できるようになる気がする。
- pythonで日常で使える自動化アプリを作ってDjangoで表示してみたい。

## 学習時間
- 10h
