## 取り組んだ課題一覧  
-a  Node.js4.5

## わかったこと
◆デバックとは、日本語で「バグを取り除く」という意味。

◆デバックの手順
①エラーの内容を把握
②原因を予想しよう
③予想の確認しよう
④解決

①<% items.foreach((item) => { %>
②foreachメソッドが間違っているかもしれない
③検索して調べる。

1.「JavaScript foreach」をWebで検索する
2.Progateのスライド検索機能でJavaScriptコースの「foreach」を検索する
④foreachではなくforEach

①1行目にはエラーの発生した場所
/home/progate/list-app/app.js:38
 );
←app.jsファイルの38行目の括弧付近

SyntaxError: Unexpected token )
SyntaxErrorは「文法エラー」という意味
「Unexpected token )」は「予期しない記号)」という意味。意訳すると「来るはずのない)が記述されている」といった意味になる。

②本来は)ではなく別の何かが来る

③connection.queryのクエリ実行後の処理であるアロー関数の閉じ括弧}が抜けている

①エラーメッセージが表示されない。

②作成機能の不具合→「/create」のルーティング部分が間違っていると予想を立てる

app.post('/create', (req, res) => {
  connection.query(
    'INSERT INTO items name VALUES (?)',
    [req.body.itemName],
    (error, results) => {
      res.redirect('/index');
    }

③メモの保存が行われていないので、データベースへの追加処理でエラーが起きている可能性が高い。

クエリの実行でエラーが起きた場合、そのエラーの内容はerrorという引数に渡される。
console.log(error);
でエラーを出力する。

You have an error in your SQL syntax
←SQLの文法に間違いがあるという意味。  

Node.js5

◆HTTPはリクエストやレスポンスなど、クライアントとサーバーが通信するときの手順やデータ形式のルール。

◆ステートレス性とは、サーバーがクライアントの状態（state）を保持しない（less）という性質のこと。
メリット：サーバーの処理速度の向上
デメリット：ログイン状態を保持できない

←これを解決するのがセッション管理

◆セッション管理とは、セッション中のクライアントの状態をサーバーが管理すること。
セッションとは、通信の始まりから終わりまでを指す。

◆セッション管理の流れ
1.クライアントの状態を登録する
2.クライアントの状態を確認する
3.クライアントの状態を破棄する

◆app.use関数は全てのリクエストに対応。
ルーティング処理の一番最初に書くことで、毎回実行することができる。受け取ることのできる引数はreq, res, nextの3つ。

◆app.use関数の引数で受け取ったnextは処理の中で関数として使うことができ、リクエストに一致する次の処理を実行することができる。

◆res.localsオブジェクトは「res.locals.プロパティ名 = 値」の形で値を代入することができる。

## 次やること
-a  Node.js5.6

## 感じたこと
-a  勉強を習慣化するとやらないと逆に不安になる。

## 学習時間
-2時間
