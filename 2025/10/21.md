## 取り組んだ課題一覧
- JavaScript

## わかったこと
```javascript
console.log("Hello World");

// 変数に代入する
let nickname = "tarou";
console.log(nickname);
nickname = "sample";
console.log(nickname);

// 定数に代入する
// 定数は変更することができないため、再代入しようとするとエラーになる
const pi = 3.14;
console.log(pi);

// プリミティブ型とオブジェクト型の違い;
// プリミティブ型の例
let str1 = "hello";
let age = 30;
let isActive = true;
let color;
let box = null;

console.log(typeof str1); // "string"
console.log(typeof age); // "number"
console.log(typeof isActive); // "boolean"
console.log(typeof color); // "undefined"
console.log(typeof box); // "object"

// オブジェクト型の例
let product = {
  name: "りんご",
  price: 200,
};

console.log(product);

// 特定のプロパティを変更して取得する
// 方法1
product.price = 250;
console.log(product.price);
// 方法2
product["price"] = 300;
console.log(product["price"]);

// 配列の例
let fruits = ["りんご", "みかん", "バナナ"];
console.log(fruits);

// indexを0から始めて特定の要素を取得する
fruits[0] = "ぶどう";
console.log(fruits[0]);

// 最後の要素を変更する
fruits[2] = "もも";
console.log(fruits[2]);

// 配列の長さを取得する
console.log(fruits.length);

// 関数の例
const outputProductInfo = (productName, productPrice) => {
    console.log("こんにちは");
    console.log(`${productName}の値段は${productPrice}円です`)
};

outputProductInfo("りんご", 200);
outputProductInfo("みかん", 150);
outputProductInfo("バナナ", 100);

// 戻り値のある関数
const add = (a, b) => {
    return a + b;
};

console.log(add(10, 20));

let x = 10;
let y = 3;
// 四則演算
console.log(x + y); // 足し算
console.log(x - y); // 引き算
console.log(x * y); // 掛け算
console.log(x / y); // 割り算
console.log(x % y); // 剰余
console.log(x ** y); // べき乗

console.log(++x); // 11
console.log(x); // 11
console.log(x++); // 11
console.log(x); // 12

console.log(--x); // 11
console.log(x); // 11
console.log(x--); // 11
console.log(x); // 10

x += 5;
console.log(x); // 15

x -= 5;
console.log(x); // 10

console.log(x > 5); // true
console.log(x < 5); // false

console.log(x >= 10); // true
console.log(x <= 5); // false

console.log(x === 10); // true
console.log(x !== 5); // true

console.log(1 === "1"); // false
console.log(1 == "1") // true

// 18歳以上なら"adult"、それ以外なら"child"を代入する
let userAge = 20;
let userType = userAge >= 18 ? "adult" : "child";
console.log(userType); // "adult"

// 論理演算子
// AND演算子
console.log(x > 5 && x < 15); // true
// OR演算子
console.log(x > 15 || x < 5); // false

// NOT演算子
console.log(!(x === 10)); // false

// falsyな値の例
undefined
null
0
""
NaN
false

// truthyな値の例
"hello"
1
[]
{}
true

console.log(true && "hello"); // "hello"
console.log(false && "hello"); // "hello"
console.log(true || "hello"); // true
console.log(false || "hello"); // "hello"

// 条件分岐の例
let age = 25;

if (age >= 20) {
  console.log("成人です");
} else if (age >= 12 && age <= 15) {
  console.log("中学生です");
} else {
  console.log("子供です");
}

// switch文の例
switch (age) {
  case 20:
    console.log("成人おめでとうございます");
    break;
  case 12:
  case 15:
  case 18:
    console.log("卒業おめでとうございます");
    break;
  default:
    console.log(`${age}歳です`);
}

// 繰り返し処理の例
// for文
for (let i = 0; i < 5; i++) {
  console.log("こんにちは");
}

// while文
let i = 0;
while (i < 5) {
  console.log("こんばんは");
  i++;
}

// for...in文
let product = {
  name: "りんご",
  price: 100,
};

for (let key in product) {
  console.log(key);
  console.log(product[key]);
}

// for...of文
let fruits = ["りんご", "みかん", "バナナ"];
for (let fruit of fruits) {
  console.log(fruit);
}

const product = {
  name: "りんご",
  price: 200,
  sayDescription: () => {
    console.log("とっても甘いです");
  },
};

product.sayDescription();

// コンストラクター関数の例
class Product {
  constructor(name, price) {
    this.name = name;
    this.price = price;
  }
  sayInfo() {
    console.log(`${this.name}の値段は${this.price}円です`);
  }
  static sayFunDetail() {
    console.log("productのオブジェクトを作るコンストラクターです");
  }
}

const p1 = new Product("りんご", 200);
const p2 = new Product("みかん", 150);
const p3 = new Product("バナナ", 100);
p1.sayInfo();
p2.sayInfo();
p3.sayInfo();
Product.sayFunDetail();

// プロパティの追加と削除
product.stock = 5;
console.log(product);

delete product.stock;
console.log(product);

// オブジェクトのコピー
const product1 = {
  name: "りんご",
  price: 100,
};

let product2 = product1;
product1.name = "みかん";

const product3 = { ...product1 };

// Mathオブジェクトの例
console.log(Math.random());
condole.log(Math.rondom());

// 四捨五入
console.log(Math.round(1.6)); // 2
// 切り上げ
console.log(Math.ceil(1.6)); // 2
// 切り捨て
console.log(Math.floor(1.6)); // 1
// 最大値
console.log(Math.max(10, 5, 20, 15)); // 20
// 最小値
console.log(Math.min(10, 5, 20, 15)); // 5

const message = "こんにちは";

// 文字列の長さを取得
console.log(message.length); // 5
// 特定の位置の文字を取得
console.log(message[1]); // ん

console.log(message.includes("にち")); // true
console.log(message.includes("さようなら")); // false

console.log(message.indexOf("ち")); // 3
console.log(message.replace("ん", "に")); // こににちは

console.log("a,b,c,d".split(",")); // ["a", "b", "c", "d"]

const now = new Date();
console.log(now);

const deta1 = new Date("2025-01-01");
console.log(date2);

const date2 = new Date(2025, 3, 1); // 2025年4月1日(月は0始まり)
console.log(date2);

console.log(date2.getFullYear()); // 2025
console.log(date2.getMonth()); // 3 (4月)
console.log(date2.getDate()); // 1
console.log(date2.getDay()); // 2 (火曜日, 0:日曜〜6:土曜)
```
## 次やること
- 
- 


## 感じたこと
- 「また1から覚えるのかあ」と思っていたけど、構造がpythonと変わりなかったからすんなり理解することができて嬉しすぎた.最初にクセの少ない、シンプルなプログラミング言語のpythonを学んで良かった

## 学習時間
- 7.05h
